# studio32-hub-live

> ⚠️ **Este repositorio contiene una build generada. No edites nada aquí a mano.**

Artefacto de despliegue de **Studio32 Hub**, el espacio de trabajo interno. Todo lo que hay dentro lo produce el proceso de build.

- **Código fuente:** [`studio32-hub`](https://github.com/tsmluky/studio32-hub)
- **Dominio:** `www.hub.studio32.es` (ver `CNAME`)
- Se sirve como PWA instalable: incluye `manifest.webmanifest` y un service worker (`sw.js`).
- `.nojekyll` desactiva el procesado de GitHub Pages.

Cualquier cambio hecho directamente en este repo se perderá en el siguiente despliegue.
